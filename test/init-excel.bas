option explicit


sub initCreationOfExcelApp(projectRootDir as variant) ' {
  '
  ' Note: the parameter(s) need to be declared as variant
  '       in Excel (apparently not so in Access).
  '

    msgBox "initCreationOfExcelApp, projectRootDir = " & projectRootDir

end sub ' }
