option explicit

sub initCreationOfWordApp(projectRootDir as string) ' {
  '
  ' Note: the parameter(s) need to be declared as variant
  '       in Excel (apparently not so in Access).
  '

    msgBox "initCreationOfWordApp, projectRootDir = " & projectRootDir

end sub ' }
